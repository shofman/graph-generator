TODO
1) Generate multiple versions of a line, and pass them along as params (to iterate through)
2) Do not rely on probability - shift to using list of possibilities
3) Change the empty spaces within the grid to be passed along as part of the recursive call